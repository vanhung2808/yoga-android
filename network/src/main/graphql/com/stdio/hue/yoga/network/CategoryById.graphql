query GetCategoryDetail($id: Int, $includeCollection: Boolean!) {
  categoryByID(id: $id) {
    name
    id
    collection @include(if: $includeCollection) {
      id
      name
      image
      description
    }
  }
}