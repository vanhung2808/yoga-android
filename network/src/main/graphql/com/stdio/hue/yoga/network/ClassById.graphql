query GetClassDetail($id:Int){
	classById(id : $id){
		name,
		id,
		description,
		video_url,
		duration,
		image,
		ability{
			id,
			name
		},
		focus{
			id,
			name
		},
		intensity{
			id,
			name
		}
	}
}